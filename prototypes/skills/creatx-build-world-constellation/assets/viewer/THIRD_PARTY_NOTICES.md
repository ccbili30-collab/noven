# Prototype third-party notices

The generated viewer loads the following upstream resource at runtime:

- `globe.gl` 2.46.1, MIT License, <https://github.com/vasturiano/globe.gl>

The dependency is not installed into CreatX or vendored by this Skill Prototype. The generated viewer requires network access for Globe.GL. The bundled starfield is a CreatX prototype asset, not an upstream package resource. This is Prototype（原型）evidence only, not a production dependency decision or Live（真实运行）integration.
