# CreatX 文档入口

## 当前权威

- `../AGENTS.md`：开发、证据、并行和维护规则。
- `../CONTEXT.md`：当前状态、已知边界和下一恢复入口。
- `../BASELINE.md`：当前仓库基线与验证范围。
- `product/creatx-product-understanding.md`：产品共识。
- `product/creatx-requirement-map.md`：产品目标、能力归属和修改位置。
- `capabilities/README.md`：Capability Line（能力线）索引。
- `adr/0005-cline-is-the-sole-agent-harness.md`：唯一 Harness（智能体运行框架）边界。

## 文档职责

- `product/`：跨能力的产品定义。
- `adr/`：难以逆转的架构决策及其理由。
- `capabilities/`：当前行为、边界、验收与实施顺序。
- `baseline/`：仍被权威文档引用的 Live（真实运行）或自动验证证据。
- `discussions/`：尚需追溯的用户意图、纠正和未决问题，不直接代表已实现。
- `plans/`：仍被当前能力线引用的未完成或恢复计划。

旧 NovelX/OpenCode 文档、被取代计划、中间试跑流水和旧视觉稿已退出活动文档树，可从标签 `pre-repository-cleanup-20260806` 恢复。完整整本 Growth 产物保留在 `../artifacts/`，不因文档清理删除。
