# CreatX 灵感库双库、百条样本与本机热度基线

日期：2026-08-10
主能力线：`workspace-ui`
规则：`WUI-034`
验收：`ACC-WUI-044`

## 已实现

- 原有题材一级筛选替换为“启发 / 幻想”两个库。
- 中文或英文问号结尾进入启发，其他陈述式点子进入幻想；本机导入使用同一规则。
- 旧 JSON `category` 字段继续兼容但不参与新分类，没有数据迁移。
- 内置样本扩充为启发50条、幻想50条，共100条。保留两条用户共创启发与原25条幻想，新增73条均有真实可访问的公共网页来源，并明确区分来源事实与重新创作内容。
- 百条样本的 ID、完整中文句、标签、来源标题、来源 URL、采集时间和整理说明继续使用现有数据结构；没有增加联网采集、账号或公共热度能力。
- 当前筛选结果按本机热度降序显示：收藏2分、喜欢1分，同分保持原顺序。页面明确标注本机热度，不显示公共计数。

## 验收

- 通过 Wikipedia 直接页面 HTTP HEAD 批量核验当前90个唯一公共来源 URL：90/90 返回 HTTP 200；两条用户共创内容不冒充网页来源。
- `bun test apps/desktop/renderer/tests/idea-library-seeds.test.ts apps/desktop/tests/creative-library-store.test.ts`：7/7，通过，621次断言。
- `bun test apps/desktop/renderer/tests`：114/114，通过，1,044次断言。
- `bun run typecheck`：通过。
- `bun run build`：通过，Main、Preload（预加载桥）和 Renderer（渲染进程）生产产物均生成。
- 专用 Web Preview（网页预览）浏览器验收：默认幻想50条、切换后启发50条；900×700下内容宽度847px、滚动宽度847px，无横向溢出，页面错误0。

## 未完成与风险

- 完整 `test:preview:web` 的旧品牌“CreatX”和旧入口“点子库”断言已同步为当前“诺文 / 灵感库”；套件随后在进入灵感库前被范围外的 Chat Studio 工作台旧结构断言阻塞（`scripts/web-preview-test.ts:109`）。本批没有扩大范围修改该结构验收。
- 未运行全仓全量、完整 Electron、Windows 打包或外部 Provider（模型服务）Live（真实运行）。
- 热度是单机个人排序，不代表公共流行度；没有账号、联网统计或跨设备同步。
- 旧导入数据没有迁移，旧 `category` 只作为兼容字段保留，页面分类由句末标点决定。

## 恢复入口

如继续推进，先在权威主线核对并移入本批文件，再单独核对 Chat Studio 当前产品结构并修正范围外旧 Preview 断言；若需要公共热度，必须先明确账号、数据来源和隐私边界，不能复用当前本机分数冒充。
